# PAGER — инвестиционная страница

Одностраничник для раннего раунда. Mobile-first, без зависимостей и сборки:
чистый HTML + CSS + vanilla JS.

```
pager-investor/
├── index.html          вся разметка + RU-тексты, EN в data-en
├── css/style.css       дизайн-система и все секции
├── js/app.js           сторискроллинг, i18n, ревилы, курсор
├── assets/img/         оптимизированные медиа (3.6 МБ на всю страницу)
└── serve.py            локальный статик-сервер для превью
```

Локальный просмотр:

```bash
python3 pager-investor/serve.py
```

Затем открыть `http://127.0.0.1:4321`. Деплой — выкладка папки как есть на любой
статик-хостинг (Netlify / Vercel / Cloudflare Pages / nginx). `serve.py` в
продакшене не нужен.

---

## Визуальный язык

Идея — **границы**. Всё держится на одной метафоре: линия между контекстами.
Отсюда волосяные хайрлайны, жёсткие стыки полос, световые кубы, порог/дверь,
кольцо-таймер. Ритм полос взят из GAZU: кремовый мастер, в который врезаны
чёрные и каменные банды с жёсткой границей.

### Палитра

| Токен | HEX | Назначение |
|---|---|---|
| `--bone` | `#EDE9E2` | мастер-фон |
| `--stone` | `#DCD6CB` | каменная полоса (статус продукта) |
| `--ink` | `#0B0B0C` | типографика и тёмные банды |
| `--personal` | `#20C997` | профиль «Личное» |
| `--work` | `#38BDF8` | профиль «Работа» |
| `--guest` | `#D4C4B7` | профиль «Гость» |
| `--alter` | `#FFCE7A` | профиль «Альтер эго» |
| `--acc-1` / `--acc-2` | `#A8BEDD` / `#C4D6F0` | акценты приложения |

Цвета профилей работают только как сигнал: полоса на карточке, свечение точки,
градиент-линейка PAGER ID, зелёный узел «Сейчас» на roadmap. Никакой заливки
крупных площадей — на кремовом они теряют контраст.

### Типографика

- **Golos Text 900** — дисплей. Uppercase, tracking `-0.03em`, line-height `0.88–0.92`.
  Широкий тяжёлый гротеск с честной кириллицей — та же логика, что у вордмарка GAZU.
- **Golos Text 400–800** — лиды и текст.
- **JetBrains Mono 400/500** — надзаголовки, номера секций, даты, PAGER ID,
  служебные строки. Tracking `0.16–0.2em`, uppercase.

Шкала: `.d-1 … .d-5` + `.d-big` (число «24»). Все размеры на `clamp()`.

---

## Структура и моушен

| # | Секция | Фон | Ключевая механика |
|---|---|---|---|
| — | Прелоадер | ink | счётчик 000→100, шторка вверх |
| — | Hero | bone | плейт с параллаксом, canvas-рой частиц, цветные импульсы потоков, построчный ревил |
| 01 | PAGER ID | bone | скрэмбл-анимация `A147 0865`, градиент-линейка профилей |
| 02 | Профили общения | ink | 4 карточки, реальные скрины UI приложения |
| 03 | Мультипрофиль | ink | **пин на 420vh**, 4 кадра `mpf-1…4` с кроссфейдом, синхронный текст и тики |
| 04 | Временные контакты | bone | кольцо на 24 деления, дуга рисуется по скроллу |
| 05 | Статус продукта | stone | реализовано / в разработке + скрины |
| 06 | Бизнес-модель | bone | B2C сплошной чёрной плашкой, B2B пунктиром — последовательность, а не паритет |
| — | Бегущая строка | ink | манифест |
| 07 | Roadmap | ink | **пин на 340vh**, горизонтальный трек, HUD `03 / 05` (десктоп); вертикальный таймлайн с растущей линией (мобильный) |
| 08 | Инвестиционный запрос | bone | список направлений, milestone, два CTA |
| — | Основатель | ink | фон «порог» |

Всё уважает `prefers-reduced-motion`. Курсор-кольцо только на десктопе с мышью.

---

## Тексты и языки

Русский — источник правды, лежит прямо в разметке. Английский — в атрибутах
`data-en` на тех же узлах; переключатель RU/EN в шапке меняет `innerHTML` без
перезагрузки. Заголовки с переносами помечены `data-split` — JS переразбивает их
на строки после смены языка, чтобы построчный ревил не ломался.

Добавить строку: положить русский текст в узел, английский — в `data-en`.
Если внутри есть `<br>`, добавить `data-split`.

---

## Что сознательно не сделано (по пометкам «без публикации» из ТЗ)

- PAGER ID не подаётся как «username вместо телефона» — отличие раскрывается
  через систему вокруг него.
- Premium ID показан как будущая модель монетизации, не как главное преимущество.
- В блоке профилей — только условия связи; имена и фото ушли в мультипрофиль.
- Нет формулировок «разные версии вас», «смена личности», «маски».
- Раздел временных контактов не называется «Общение в цифрах»; «24 часа» подано
  как характеристика продукта. Про удаление данных не обещано ничего — только
  «завершить связь».
- Направления бизнес-модели разведены по этапам, а не выложены в один ряд.
- Roadmap показывает продуктовые результаты, а не список функций.

## Что нужно заменить перед публикацией

- `hello@pager.app` в CTA и футере — на реальный адрес или форму.
- `og:image` — на отрисованную превью-картинку 1200×630.
- Даты в roadmap — обновлять при любом сдвиге сроков (устаревшая дата вредит
  сильнее, чем её отсутствие).
